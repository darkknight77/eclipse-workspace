package com.testing.org;

import java.util.ArrayList;

public class ArmstrongNum {

	// 0, 1, 153, 370, 371 and 407
	
	public static ArrayList<Integer> getArmstrongNums(int count) {
	
		ArrayList<Integer> armstrongNums= new ArrayList<>();
		int  r = 0, s = 0, out = 0, temp=0;
		for (int i = 0; i < count; i++) {
			
			temp=i;
			
			while (temp > 0) {
				r = temp % 10;
				temp = temp / 10;
				s = s * 10 + r;
				out += r * r * r;

			}

			if (out == i) armstrongNums.add(i);

		}
		return armstrongNums;
		
	}
	

	public static String isArmstrongNum(int num) {

		int temp = num, r = 0, s = 0, out = 0;

		while (temp > 0) {
			r = temp % 10;
			temp = temp / 10;
			s = s * 10 + r;
			out += r * r * r;

		}

		if (out == num)
			return num + " is a Armstrong Number";
		else
			return num + " is not a Armstrong Number";

	}

	public static void main(String[] args) {

		System.out.println(isArmstrongNum(360));
		System.out.println(getArmstrongNums(1000));
		
	}

}
